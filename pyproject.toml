[project]
name = "stride"
version = "0.1.0"
description = "STRIDE — Field-Aware Integer Coder"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
