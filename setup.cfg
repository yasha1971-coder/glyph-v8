[metadata]
name = stride
version = 0.1.0
description = STRIDE — Field-Aware Integer Coder

[options]
packages = find:
python_requires = >=3.8

[options.entry_points]
console_scripts =
    stride = stride.cli:main
